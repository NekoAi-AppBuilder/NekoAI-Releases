# v0.4.40 — Provider modal wheel scroll fix

The provider modal scroll container now handles mouse-wheel input directly. Wheel scrolling works even when the pointer is over provider rows/buttons, while preserving the existing scrollbar and layout.

No provider connection, model, icon, or layout behavior was changed.
